# Assembly diagram update — 25 September 2026

The three SVG illustrations were revised against `IMG_3895.jpg` (separate kit parts), `IMG_3898.jpg` (assembled mount), and the supplied clarification of the strap and sensor directions.

- `kit-clips.svg`: vented housing with visibly curved back; two clips, retaining flanges, open slots, and arrows showing one clip fitting each side.
- `kit-saddle.svg`: matching housing and saddle curves. The assembled section uses a shared boundary at the saddle/housing interface, with no artificial gap. The oar rests against the concave saddle face. The exploded view omits clips for clarity.
- `kit-strap.svg`: wrap around the oar, thread each clip slot from inside to outside, then fold the tail back on that same side. The enlarged cross-section separates the overlapping layers slightly so the fastening faces can be seen. Hooks and loops share one face; the opposite back is smooth.
- Kit instructions explicitly state that sensor +Y points toward the handle, and the offset button toward the blade is in the sensor's −Y direction. With the oar vertical and blade down, +Y points upward.

These are assembly illustrations, not dimensioned manufacturing drawings. Existing cleaned photographs remain unchanged. No generative image edit was used for this update.

Validation: all three SVGs parsed as XML and were rendered to PNG with Sharp for visual inspection. The illustrations and local image references in `instructions.html` were checked; combined-page and responsive checks are part of the website release validation.
